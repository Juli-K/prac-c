#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    unsigned int n;
    int k;
    scanf("%u %d", &n, &k);
    for ( int i = 0; i < k; i++)
    {
        n = n >> 1;
    }
    printf ("%u", n);
    return 0;
}
